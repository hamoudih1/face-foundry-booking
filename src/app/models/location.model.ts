export class LocationItem {
    name: string;
    address: string;
    imgURL: string;

    constructor(name: string, address: string, imgURL: string) {
        this.name = name;
        this.address = address;
        this.imgURL = imgURL;
    }
}
